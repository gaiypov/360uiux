# 360° РАБОТА - Admin Dashboard

Панель модератора для управления платформой 360° РАБОТА.

## 🎯 Основные функции

- **Модерация вакансий** - Проверка и одобрение новых вакансий
- **Модерация пользователей** - Проверка новых регистраций
- **Статистика** - Мониторинг активности платформы
- **Безопасность** - Блокировка нарушителей

## 🚀 Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Lucide React** - Иконки
- **Recharts** - Графики и аналитика

## 📦 Установка

```bash
# Установить зависимости
npm install

# Запустить dev сервер (порт 3001)
npm run dev

# Собрать для продакшена
npm run build

# Запустить продакшен
npm start
```

## ⚙️ Конфигурация

Создайте файл `.env.local` с переменными окружения:

```env
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
```

## 🔐 Аутентификация

Админ панель требует авторизацию с ролью `moderator`.

**Доступ к API:**
- Все запросы отправляются с JWT токеном в заголовке `Authorization: Bearer <token>`
- Токен получается при входе через `/auth/moderator/login`

## 📱 Страницы

- `/` - Главная панель с статистикой
- `/moderation/vacancies` - Модерация вакансий
- `/moderation/users` - Модерация пользователей
- `/security` - Безопасность и блокировки
- `/settings` - Настройки

## 🎨 Дизайн

Премиальный темный дизайн в стиле Revolut Ultra:
- Стеклянные карточки с blur эффектом
- Платиновые градиенты
- Минималистичный интерфейс
- Адаптивная верстка

## 🔗 Интеграция с Backend

API интеграция через `src/lib/api.ts`:

```typescript
import { api } from '@/lib/api';

// Получить статистику
const stats = await api.getStats();

// Одобрить вакансию
await api.approveVacancy(vacancyId);

// Заблокировать пользователя
await api.banUser(userId, reason);
```

## 📝 Структура проекта

```
admin-dashboard/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── layout.tsx         # Главный layout
│   │   ├── page.tsx           # Главная страница
│   │   ├── moderation/        # Модерация
│   │   │   ├── vacancies/     # Вакансии
│   │   │   └── users/         # Пользователи
│   │   └── globals.css        # Глобальные стили
│   ├── components/            # React компоненты
│   │   ├── ui/               # UI компоненты
│   │   ├── Sidebar.tsx       # Боковое меню
│   │   └── Header.tsx        # Шапка
│   └── lib/                  # Утилиты
│       ├── api.ts           # API клиент
│       └── utils.ts         # Хелперы
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.js
```

## 👨‍💼 Роли модератора

- ✅ Одобрение/отклонение вакансий
- ✅ Одобрение/отклонение пользователей
- ✅ Блокировка нарушителей
- ✅ Просмотр статистики
- ❌ Редактирование контента (только модерация)

## 🛡️ Безопасность

- JWT токены для аутентификации
- Роль `moderator` обязательна
- Все модерационные действия логируются в backend
- Rate limiting на API запросах

## 📊 Метрики

Админ панель отображает:
- Количество вакансий на модерации
- Количество пользователей на проверке
- Регистрации за сегодня
- Новые вакансии за сегодня
- Общая статистика платформы
