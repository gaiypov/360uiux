# 🛡️ Админ панель (MODERATOR)

> **⚠️ ВАЖНО:** Это админ панель для модераторов, НЕ личный кабинет работодателя!
> Кабинет работодателя находится в `src/screens/employer/`

## 📋 Описание

Админ панель для модераторов системы 360° РАБОТА. Предоставляет полный доступ к управлению платформой.

## 🎯 Роль пользователя

- **Роль:** `MODERATOR` (не `EMPLOYER`!)
- **Доступ:** Только для модераторов системы
- **Навигатор:** `AdminNavigator.tsx`
- **API:** `/api/v1/admin/*`

## 📱 Экраны

### 1. AdminDashboardScreen.tsx
**Главный экран с аналитикой**

Функционал:
- Статистика системы (пользователи, вакансии, отклики)
- Финансовая сводка
- Недавние жалобы
- Быстрые действия

### 2. AdminUsersScreen.tsx
**Управление ВСЕМИ пользователями**

Функционал:
- Список всех пользователей (соискатели + работодатели + модераторы)
- Фильтры по роли
- Поиск по имени/телефону
- Верификация пользователей
- Удаление пользователей
- Просмотр статистики пользователя

### 3. AdminVacanciesScreen.tsx
**Модерация ВСЕХ вакансий в системе**

Функционал:
- Список всех вакансий
- Фильтры по статусу (черновик, опубликована, архив)
- Модерация вакансий
- Размещение в топе
- Изменение статуса
- Удаление

### 4. AdminReportsScreen.tsx
**Рассмотрение жалоб на контент**

Функционал:
- Список жалоб на видео
- Фильтры по статусу (на рассмотрении, одобрено, отклонено)
- Просмотр деталей жалобы
- Одобрение/отклонение жалоб
- Блокировка видео
- Комментарии модератора

### 5. AdminTransactionsScreen.tsx
**Просмотр ВСЕХ транзакций системы**

Функционал:
- Список всех транзакций
- Фильтры по типу (пополнение, оплата, возврат, вывод)
- Фильтры по статусу (в обработке, завершено, ошибка)
- Финансовая сводка (выручка, списания, чистая прибыль)
- Просмотр деталей транзакции
- Топ-спендеры (работодатели)

### 6. AdminSettingsScreen.tsx
**Глобальные настройки системы**

Функционал:
- Автомодерация (вкл/выкл)
- Лимит просмотров для гостей
- Лимит просмотров видео-резюме
- Стоимость топ вакансии
- Минимальная сумма вывода
- Системная информация

## 🎨 Дизайн

- **Стиль:** Revolut Ultra
- **Тема:** Темная с glassmorphism
- **Иконки:** Material Community Icons
- **Навигация:** Bottom Tabs + Stack

## 🔐 Безопасность

- Доступ только для пользователей с ролью `MODERATOR`
- Все API endpoints защищены middleware
- Backend: `backend/src/controllers/AdminController.ts`
- Routes: `backend/src/routes/admin.routes.ts`

## 🚀 API Endpoints

```typescript
// Получение статистики дашборда
GET /api/v1/admin/dashboard/stats

// Управление пользователями
GET /api/v1/admin/users
PUT /api/v1/admin/users/:id
DELETE /api/v1/admin/users/:id

// Управление вакансиями
GET /api/v1/admin/vacancies
PUT /api/v1/admin/vacancies/:id
DELETE /api/v1/admin/vacancies/:id

// Рассмотрение жалоб
GET /api/v1/admin/complaints
POST /api/v1/admin/complaints/:id/process

// Финансы
GET /api/v1/admin/financial/stats
GET /api/v1/admin/financial/transactions
GET /api/v1/admin/financial/transactions/:id

// Настройки
GET /api/v1/admin/settings
PUT /api/v1/admin/settings
```

## 📊 Типы данных

Все типы определены в `src/types/index.ts`:

```typescript
- AdminDashboardStats
- AdminUser
- AdminVacancy
- AdminComplaint
- AdminSettings
- AdminFinancialStats
- AdminTransaction
- TopSpender
```

## 🔧 Оптимизации

**Backend:**
- ✅ Исправлена N+1 проблема в getFinancialStats()
- ✅ Оптимизированы database queries (с 10+ до 2 запросов)
- ✅ Добавлены null checks и валидация

**Frontend:**
- ✅ React.memo для оптимизации re-renders
- ✅ useCallback для всех async функций
- ✅ useEffect cleanup для предотвращения memory leaks
- ✅ useWindowDimensions вместо Dimensions.get()

## 📝 Примеры использования

### Навигация к экрану админ панели

```typescript
// Из любого места в AdminNavigator
navigation.navigate('AdminUsers');
navigation.navigate('AdminVacancies');
navigation.navigate('AdminReports');
navigation.navigate('AdminTransactions');
navigation.navigate('AdminSettings');
```

### Использование adminApi

```typescript
import { adminApi } from '@/services/adminApi';

// Получить статистику
const stats = await adminApi.getDashboardStats();

// Получить пользователей
const users = await adminApi.getUsers({
  page: 1,
  limit: 50,
  role: 'EMPLOYER'
});

// Верифицировать пользователя
await adminApi.updateUser(userId, { verified: true });
```

## ⚠️ Отличия от кабинета работодателя

| Админ панель (MODERATOR) | Кабинет работодателя (EMPLOYER) |
|--------------------------|----------------------------------|
| Все пользователи | Только кандидаты на свои вакансии |
| Все вакансии | Только свои вакансии |
| Все транзакции | Только свой кошелек |
| Глобальные настройки | Настройки профиля |
| Модерация контента | Создание вакансий |
| Системная аналитика | Аналитика своих вакансий |

## 🛠️ Разработка

При добавлении новых функций:
1. Добавить endpoint в `backend/src/controllers/AdminController.ts`
2. Добавить route в `backend/src/routes/admin.routes.ts`
3. Добавить метод в `src/services/adminApi.ts`
4. Добавить типы в `src/types/index.ts`
5. Создать/обновить экран в `src/screens/admin/`

---

**Последнее обновление:** 2025-11-13
**Версия:** 1.0
**Статус:** Полностью оптимизирован ✅
