# 360° РАБОТА - Cursor AI Rules

## 🎯 О проекте

Это **React Native приложение** для поиска работы в стиле **Revolut Ultra** с видео-резюме и TikTok-style лентой вакансий.

**Tech Stack:**
- Frontend: React Native + TypeScript + Zustand
- Backend: Node.js + Express + Prisma + PostgreSQL
- Design: Revolut Ultra (glassmorphism, металлические эффекты)
- Video: api.video integration

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО: 3 РОЛИ ПОЛЬЗОВАТЕЛЕЙ

### Не путай эти роли!

#### 1. 👤 JOBSEEKER (Соискатель)
- **Роль:** `'jobseeker'`
- **Навигатор:** `JobSeekerNavigator.tsx`
- **Директория:** `src/screens/` (общие экраны)
- **Что делает:** Ищет работу, создает резюме с видео, откликается на вакансии

#### 2. 💼 EMPLOYER (Работодатель)
- **Роль:** `'employer'`
- **Навигатор:** `EmployerNavigator.tsx`
- **Директория:** `src/screens/employer/`
- **API:** `/api/v1/employer/*`
- **Что делает:** Управляет СВОИМИ вакансиями, смотрит кандидатов на СВОИ вакансии
- **⚠️ ЭТО НЕ АДМИН ПАНЕЛЬ!** Это личный кабинет работодателя

#### 3. 🛡️ MODERATOR (Администратор/Модератор)
- **Роль:** `'moderator'`
- **Навигатор:** `AdminNavigator.tsx`
- **Директория:** `src/screens/admin/`
- **API:** `/api/v1/admin/*`
- **Что делает:** Модерирует ВЕСЬ контент, управляет ВСЕМИ пользователями, видит ВСЕ транзакции
- **⚠️ ЭТО АДМИН ПАНЕЛЬ!** Не путай с кабинетом работодателя

---

## 📂 Структура проекта

### Frontend структура:
```
src/
├── navigation/
│   ├── RootNavigator.tsx        # Главный роутер (проверка ролей)
│   ├── JobSeekerNavigator.tsx   # 👤 Навигация соискателя
│   ├── EmployerNavigator.tsx    # 💼 Навигация работодателя (НЕ АДМИН!)
│   └── AdminNavigator.tsx       # 🛡️ Админ панель (MODERATOR)
│
├── screens/
│   ├── admin/                   # 🛡️ АДМИН ПАНЕЛЬ (для модераторов)
│   │   ├── AdminDashboardScreen.tsx
│   │   ├── AdminUsersScreen.tsx
│   │   ├── AdminVacanciesScreen.tsx
│   │   ├── AdminReportsScreen.tsx
│   │   ├── AdminTransactionsScreen.tsx
│   │   └── AdminSettingsScreen.tsx
│   │
│   ├── employer/                # 💼 КАБИНЕТ РАБОТОДАТЕЛЯ (НЕ АДМИН!)
│   │   ├── CreateVacancyScreen.tsx
│   │   ├── CandidatesScreen.tsx
│   │   ├── AnalyticsScreen.tsx
│   │   └── ... (другие экраны работодателя)
│   │
│   └── ... (общие экраны для соискателей)
│
└── services/
    ├── api.ts              # Общий API клиент
    └── adminApi.ts         # 🛡️ API только для админов
```

### Backend структура:
```
backend/src/
├── controllers/
│   ├── AdminController.ts       # 🛡️ Контроллер АДМИН ПАНЕЛИ
│   ├── AuthController.ts
│   ├── VacancyController.ts
│   └── ... (другие контроллеры)
│
├── routes/
│   ├── admin.routes.ts          # 🛡️ Routes админ панели
│   └── ... (другие routes)
│
└── middleware/
    └── authenticateToken.ts     # JWT проверка
```

---

## 🚨 ГЛАВНЫЕ ПРАВИЛА

### Правило 1: НЕ ПУТАЙ EMPLOYER И MODERATOR!

**НЕПРАВИЛЬНО:**
```typescript
// ❌ Путаница!
if (user.role === 'employer') {
  navigation.navigate('AdminDashboard'); // ОШИБКА!
}
```

**ПРАВИЛЬНО:**
```typescript
// ✅ Правильно
if (user.role === 'moderator') {
  navigation.navigate('AdminDashboard'); // Админ панель
}

if (user.role === 'employer') {
  navigation.navigate('CreateVacancy'); // Кабинет работодателя
}
```

### Правило 2: Правильные директории

**Админ панель (MODERATOR):**
- ✅ Ищи в: `src/screens/admin/`
- ✅ API: `src/services/adminApi.ts`
- ✅ Backend: `backend/src/controllers/AdminController.ts`
- ✅ Routes: `backend/src/routes/admin.routes.ts`

**Кабинет работодателя (EMPLOYER):**
- ✅ Ищи в: `src/screens/employer/`
- ✅ API: будет в `src/services/api.ts` или отдельный employerApi
- ✅ Backend: соответствующие контроллеры (не Admin!)

### Правило 3: API Endpoints

**Админ API (только для moderator):**
```
/api/v1/admin/*
- GET /api/v1/admin/dashboard/stats
- GET /api/v1/admin/users (ВСЕ пользователи)
- GET /api/v1/admin/vacancies (ВСЕ вакансии)
- GET /api/v1/admin/financial/transactions (ВСЕ транзакции)
```

**Employer API:**
```
/api/v1/employer/*
- GET /api/v1/employer/vacancies (ТОЛЬКО свои)
- GET /api/v1/employer/applications (ТОЛЬКО свои)
```

---

## 📋 Проверка перед ответом

Перед тем как давать ответ, проверь:

1. **Какую роль имеет в виду пользователь?**
   - Админ панель → MODERATOR → `src/screens/admin/`
   - Кабинет работодателя → EMPLOYER → `src/screens/employer/`

2. **Правильную ли директорию ты используешь?**
   ```bash
   # Админ панель
   ls src/screens/admin/

   # Кабинет работодателя
   ls src/screens/employer/
   ```

3. **Правильный ли API endpoint?**
   - Админ → `/api/v1/admin/*`
   - Работодатель → `/api/v1/employer/*`

---

## 📚 Документация

**ВСЕГДА читай эти файлы перед работой:**

1. `ADMIN_PANEL_MANIFEST.md` - полный список файлов админ панели
2. `NAVIGATION_STRUCTURE.md` - структура навигации для всех ролей
3. `src/screens/admin/README.md` - документация админ панели
4. `src/screens/employer/README.md` - документация кабинета работодателя

---

## 🔍 Команды для проверки

Если не уверен где искать код:

```bash
# Проверить текущую ветку
git branch --show-current

# Найти все файлы админки
find src/screens/admin -type f

# Найти все файлы работодателя
find src/screens/employer -type f

# Проверить админ API
cat src/services/adminApi.ts

# Проверить админ контроллер
cat backend/src/controllers/AdminController.ts
```

---

## 🎨 Стиль кода

### React Native компоненты:
- ✅ Используй TypeScript
- ✅ Используй функциональные компоненты с hooks
- ✅ useCallback для функций в useEffect dependencies
- ✅ React.memo для оптимизации списков
- ✅ Cleanup функции в useEffect (mounted flag)
- ✅ useWindowDimensions вместо Dimensions.get()

**Пример оптимизированного компонента:**
```typescript
import React, { useEffect, useState, useCallback } from 'react';

export function MyScreen() {
  const [data, setData] = useState([]);
  const { showToast } = useToastStore();

  const loadData = useCallback(async () => {
    try {
      const result = await api.getData();
      setData(result);
    } catch (error) {
      showToast('error', 'Ошибка загрузки');
    }
  }, [showToast]);

  useEffect(() => {
    let mounted = true;

    const fetchData = async () => {
      const result = await api.getData();
      if (mounted) {
        setData(result);
      }
    };

    fetchData();

    return () => {
      mounted = false; // cleanup
    };
  }, []);

  // Мемоизированный компонент
  const ListItem = React.memo(({ item }) => (
    <View>{item.name}</View>
  ));

  return <FlatList data={data} renderItem={ListItem} />;
}
```

### Backend код:
- ✅ Проверяй роли в middleware
- ✅ Используй Prisma для БД
- ✅ Избегай N+1 queries
- ✅ Валидация данных
- ✅ Обработка ошибок

---

## 🛡️ Безопасность

### Всегда проверяй роли:

```typescript
// Frontend
const { user } = useAuthStore();
if (user?.role !== 'moderator') {
  // Запрети доступ к админке
}

// Backend (middleware)
export const requireModerator = (req, res, next) => {
  if (req.user.role !== 'moderator') {
    return res.status(403).json({ error: 'Access denied' });
  }
  next();
};
```

---

## 📊 Текущий статус проекта

### ✅ Полностью готово:
- Админ панель (6 экранов) - `src/screens/admin/`
- Админ API клиент - `src/services/adminApi.ts`
- Админ backend - `backend/src/controllers/AdminController.ts`
- Навигация с проверкой ролей
- JWT аутентификация

### 🚧 В разработке:
- Некоторые экраны работодателя (employer)
- Некоторые экраны соискателя

---

## 💡 Примеры типичных задач

### "Добавить новую функцию в админ панель"
1. Определи экран: `src/screens/admin/Admin...Screen.tsx`
2. Добавь метод в API: `src/services/adminApi.ts`
3. Добавь endpoint: `backend/src/controllers/AdminController.ts`
4. Добавь route: `backend/src/routes/admin.routes.ts`

### "Добавить функцию для работодателя"
1. ⚠️ НЕ используй admin! Используй `src/screens/employer/`
2. Создай/обнови экран в `employer/`
3. API endpoint должен быть `/api/v1/employer/*`

### "Показать статистику пользователю"
1. Определи роль:
   - JOBSEEKER → свою статистику
   - EMPLOYER → статистику своих вакансий
   - MODERATOR → статистику всей системы

---

## 🎯 Финальный чек-лист

Перед каждым ответом:

- [ ] Я понимаю какая роль (`jobseeker` / `employer` / `moderator`)
- [ ] Я знаю где искать код (правильная директория)
- [ ] Я не путаю админ панель с кабинетом работодателя
- [ ] Я прочитал документацию (`ADMIN_PANEL_MANIFEST.md`)
- [ ] Я использую правильный API endpoint

---

**Версия:** 1.0
**Дата:** 2025-11-13
**Ветка с админкой:** `claude/admin-panel-cursor-fix-011CV4x8BWYuBAWUkcSyYcJh`

**Если что-то непонятно → читай документацию в корне проекта!**
